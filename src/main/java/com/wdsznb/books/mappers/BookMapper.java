package com.wdsznb.books.mappers;

import com.wdsznb.books.beans.BookBean;
import org.apache.ibatis.annotations.Insert;
import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Select;
import org.springframework.stereotype.Repository;

/**
 * Created by ${wanghongjie} on 2017/5/17.
 */
//@Repository
//public interface BookMapper {
//    @Select("SELECT bamount FROM books WHERE bname=#{bname}")
//    public int getBookamount(BookBean bookBean);
//
//    @Insert("INSERT INTO lendbooks (bid,uid,lenddate) values(#{bid},#{uid},#{lenddate}")
//    public int borrowBook(lendbookBean lendbook);
//
//
//
//}
